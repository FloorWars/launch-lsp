/* Autogenerated file. Do not edit manually. */
/* tslint:disable */
/* eslint-disable */

import {
  ethers,
  EventFilter,
  Signer,
  BigNumber,
  BigNumberish,
  PopulatedTransaction,
  Contract,
  ContractTransaction,
  Overrides,
  CallOverrides,
} from "ethers";
import { BytesLike } from "@ethersproject/bytes";
import { Listener, Provider } from "@ethersproject/providers";
import { FunctionFragment, EventFragment, Result } from "@ethersproject/abi";
import { TypedEventFilter, TypedEvent, TypedListener } from "./commons";

interface UniswapV2BrokerInterface extends ethers.utils.Interface {
  functions: {
    "swapToPrice(bool,address,address,address[2],uint256[2],uint256[2],address,uint256)": FunctionFragment;
    "computeTradeToMoveMarket(uint256,uint256,uint256,uint256)": FunctionFragment;
    "getReserves(address,address,address)": FunctionFragment;
  };

  encodeFunctionData(
    functionFragment: "swapToPrice",
    values: [
      boolean,
      string,
      string,
      [string, string],
      [BigNumberish, BigNumberish],
      [BigNumberish, BigNumberish],
      string,
      BigNumberish
    ]
  ): string;
  encodeFunctionData(
    functionFragment: "computeTradeToMoveMarket",
    values: [BigNumberish, BigNumberish, BigNumberish, BigNumberish]
  ): string;
  encodeFunctionData(
    functionFragment: "getReserves",
    values: [string, string, string]
  ): string;

  decodeFunctionResult(
    functionFragment: "swapToPrice",
    data: BytesLike
  ): Result;
  decodeFunctionResult(
    functionFragment: "computeTradeToMoveMarket",
    data: BytesLike
  ): Result;
  decodeFunctionResult(
    functionFragment: "getReserves",
    data: BytesLike
  ): Result;

  events: {};
}

export class UniswapV2Broker extends Contract {
  connect(signerOrProvider: Signer | Provider | string): this;
  attach(addressOrName: string): this;
  deployed(): Promise<this>;

  listeners<EventArgsArray extends Array<any>, EventArgsObject>(
    eventFilter?: TypedEventFilter<EventArgsArray, EventArgsObject>
  ): Array<TypedListener<EventArgsArray, EventArgsObject>>;
  off<EventArgsArray extends Array<any>, EventArgsObject>(
    eventFilter: TypedEventFilter<EventArgsArray, EventArgsObject>,
    listener: TypedListener<EventArgsArray, EventArgsObject>
  ): this;
  on<EventArgsArray extends Array<any>, EventArgsObject>(
    eventFilter: TypedEventFilter<EventArgsArray, EventArgsObject>,
    listener: TypedListener<EventArgsArray, EventArgsObject>
  ): this;
  once<EventArgsArray extends Array<any>, EventArgsObject>(
    eventFilter: TypedEventFilter<EventArgsArray, EventArgsObject>,
    listener: TypedListener<EventArgsArray, EventArgsObject>
  ): this;
  removeListener<EventArgsArray extends Array<any>, EventArgsObject>(
    eventFilter: TypedEventFilter<EventArgsArray, EventArgsObject>,
    listener: TypedListener<EventArgsArray, EventArgsObject>
  ): this;
  removeAllListeners<EventArgsArray extends Array<any>, EventArgsObject>(
    eventFilter: TypedEventFilter<EventArgsArray, EventArgsObject>
  ): this;

  listeners(eventName?: string): Array<Listener>;
  off(eventName: string, listener: Listener): this;
  on(eventName: string, listener: Listener): this;
  once(eventName: string, listener: Listener): this;
  removeListener(eventName: string, listener: Listener): this;
  removeAllListeners(eventName?: string): this;

  queryFilter<EventArgsArray extends Array<any>, EventArgsObject>(
    event: TypedEventFilter<EventArgsArray, EventArgsObject>,
    fromBlockOrBlockhash?: string | number | undefined,
    toBlock?: string | number | undefined
  ): Promise<Array<TypedEvent<EventArgsArray & EventArgsObject>>>;

  interface: UniswapV2BrokerInterface;

  functions: {
    /**
     * True price is expressed in the ratio of token A to token B.The caller must approve this contract to spend whichever token is intended to be swapped.
     * Swaps an amount of either token such that the trade results in the uniswap pair's price being as close as possible to the truePrice.
     * @param deadline to limit when the trade can execute. If the tx is mined after this timestamp then revert.
     * @param maxSpendTokens array of unit to represent the max to spend in the two tokens.
     * @param swappedTokens array of addresses which are to be swapped. The order does not matter as the function will figure out which tokens need to be exchanged to move the market to the desired "true" price.
     * @param to recipient of the trade proceeds.
     * @param tradingAsEOA bool to indicate if the UniswapV2Broker is being called by a DSProxy or an EOA.
     * @param truePriceTokens array of unit used to represent the true price. 0th value is the numerator of the true price and the 1st value is the the denominator of the true price.
     * @param uniswapFactory address of the uniswap factory used to fetch current pair reserves.
     * @param uniswapRouter address of the uniswap router used to facilitate trades.
     */
    swapToPrice(
      tradingAsEOA: boolean,
      uniswapRouter: string,
      uniswapFactory: string,
      swappedTokens: [string, string],
      truePriceTokens: [BigNumberish, BigNumberish],
      maxSpendTokens: [BigNumberish, BigNumberish],
      to: string,
      deadline: BigNumberish,
      overrides?: Overrides & { from?: string | Promise<string> }
    ): Promise<ContractTransaction>;

    /**
     * True price is expressed in the ratio of token A to token B.The caller must approve this contract to spend whichever token is intended to be swapped.
     * Swaps an amount of either token such that the trade results in the uniswap pair's price being as close as possible to the truePrice.
     * @param deadline to limit when the trade can execute. If the tx is mined after this timestamp then revert.
     * @param maxSpendTokens array of unit to represent the max to spend in the two tokens.
     * @param swappedTokens array of addresses which are to be swapped. The order does not matter as the function will figure out which tokens need to be exchanged to move the market to the desired "true" price.
     * @param to recipient of the trade proceeds.
     * @param tradingAsEOA bool to indicate if the UniswapV2Broker is being called by a DSProxy or an EOA.
     * @param truePriceTokens array of unit used to represent the true price. 0th value is the numerator of the true price and the 1st value is the the denominator of the true price.
     * @param uniswapFactory address of the uniswap factory used to fetch current pair reserves.
     * @param uniswapRouter address of the uniswap router used to facilitate trades.
     */
    "swapToPrice(bool,address,address,address[2],uint256[2],uint256[2],address,uint256)"(
      tradingAsEOA: boolean,
      uniswapRouter: string,
      uniswapFactory: string,
      swappedTokens: [string, string],
      truePriceTokens: [BigNumberish, BigNumberish],
      maxSpendTokens: [BigNumberish, BigNumberish],
      to: string,
      deadline: BigNumberish,
      overrides?: Overrides & { from?: string | Promise<string> }
    ): Promise<ContractTransaction>;

    /**
     * Note that this method uses the Babylonian square root method which has a small margin of error which will result in a small over or under estimation on the size of the trade needed.
     * Given the "true" price a token (represented by truePriceTokenA/truePriceTokenB) and the reservers in the uniswap pair, calculate: a) the direction of trade (aToB) and b) the amount needed to trade (amountIn) to move the pool price to be equal to the true price.
     * @param reserveA number of token A in the pair reserves
     * @param reserveB number of token B in the pair reserves
     * @param truePriceTokenA the nominator of the true price.
     * @param truePriceTokenB the denominator of the true price.
     */
    computeTradeToMoveMarket(
      truePriceTokenA: BigNumberish,
      truePriceTokenB: BigNumberish,
      reserveA: BigNumberish,
      reserveB: BigNumberish,
      overrides?: CallOverrides
    ): Promise<[boolean, BigNumber] & { aToB: boolean; amountIn: BigNumber }>;

    /**
     * Note that this method uses the Babylonian square root method which has a small margin of error which will result in a small over or under estimation on the size of the trade needed.
     * Given the "true" price a token (represented by truePriceTokenA/truePriceTokenB) and the reservers in the uniswap pair, calculate: a) the direction of trade (aToB) and b) the amount needed to trade (amountIn) to move the pool price to be equal to the true price.
     * @param reserveA number of token A in the pair reserves
     * @param reserveB number of token B in the pair reserves
     * @param truePriceTokenA the nominator of the true price.
     * @param truePriceTokenB the denominator of the true price.
     */
    "computeTradeToMoveMarket(uint256,uint256,uint256,uint256)"(
      truePriceTokenA: BigNumberish,
      truePriceTokenB: BigNumberish,
      reserveA: BigNumberish,
      reserveB: BigNumberish,
      overrides?: CallOverrides
    ): Promise<[boolean, BigNumber] & { aToB: boolean; amountIn: BigNumber }>;

    getReserves(
      factory: string,
      tokenA: string,
      tokenB: string,
      overrides?: CallOverrides
    ): Promise<
      [BigNumber, BigNumber] & { reserveA: BigNumber; reserveB: BigNumber }
    >;

    "getReserves(address,address,address)"(
      factory: string,
      tokenA: string,
      tokenB: string,
      overrides?: CallOverrides
    ): Promise<
      [BigNumber, BigNumber] & { reserveA: BigNumber; reserveB: BigNumber }
    >;
  };

  /**
   * True price is expressed in the ratio of token A to token B.The caller must approve this contract to spend whichever token is intended to be swapped.
   * Swaps an amount of either token such that the trade results in the uniswap pair's price being as close as possible to the truePrice.
   * @param deadline to limit when the trade can execute. If the tx is mined after this timestamp then revert.
   * @param maxSpendTokens array of unit to represent the max to spend in the two tokens.
   * @param swappedTokens array of addresses which are to be swapped. The order does not matter as the function will figure out which tokens need to be exchanged to move the market to the desired "true" price.
   * @param to recipient of the trade proceeds.
   * @param tradingAsEOA bool to indicate if the UniswapV2Broker is being called by a DSProxy or an EOA.
   * @param truePriceTokens array of unit used to represent the true price. 0th value is the numerator of the true price and the 1st value is the the denominator of the true price.
   * @param uniswapFactory address of the uniswap factory used to fetch current pair reserves.
   * @param uniswapRouter address of the uniswap router used to facilitate trades.
   */
  swapToPrice(
    tradingAsEOA: boolean,
    uniswapRouter: string,
    uniswapFactory: string,
    swappedTokens: [string, string],
    truePriceTokens: [BigNumberish, BigNumberish],
    maxSpendTokens: [BigNumberish, BigNumberish],
    to: string,
    deadline: BigNumberish,
    overrides?: Overrides & { from?: string | Promise<string> }
  ): Promise<ContractTransaction>;

  /**
   * True price is expressed in the ratio of token A to token B.The caller must approve this contract to spend whichever token is intended to be swapped.
   * Swaps an amount of either token such that the trade results in the uniswap pair's price being as close as possible to the truePrice.
   * @param deadline to limit when the trade can execute. If the tx is mined after this timestamp then revert.
   * @param maxSpendTokens array of unit to represent the max to spend in the two tokens.
   * @param swappedTokens array of addresses which are to be swapped. The order does not matter as the function will figure out which tokens need to be exchanged to move the market to the desired "true" price.
   * @param to recipient of the trade proceeds.
   * @param tradingAsEOA bool to indicate if the UniswapV2Broker is being called by a DSProxy or an EOA.
   * @param truePriceTokens array of unit used to represent the true price. 0th value is the numerator of the true price and the 1st value is the the denominator of the true price.
   * @param uniswapFactory address of the uniswap factory used to fetch current pair reserves.
   * @param uniswapRouter address of the uniswap router used to facilitate trades.
   */
  "swapToPrice(bool,address,address,address[2],uint256[2],uint256[2],address,uint256)"(
    tradingAsEOA: boolean,
    uniswapRouter: string,
    uniswapFactory: string,
    swappedTokens: [string, string],
    truePriceTokens: [BigNumberish, BigNumberish],
    maxSpendTokens: [BigNumberish, BigNumberish],
    to: string,
    deadline: BigNumberish,
    overrides?: Overrides & { from?: string | Promise<string> }
  ): Promise<ContractTransaction>;

  /**
   * Note that this method uses the Babylonian square root method which has a small margin of error which will result in a small over or under estimation on the size of the trade needed.
   * Given the "true" price a token (represented by truePriceTokenA/truePriceTokenB) and the reservers in the uniswap pair, calculate: a) the direction of trade (aToB) and b) the amount needed to trade (amountIn) to move the pool price to be equal to the true price.
   * @param reserveA number of token A in the pair reserves
   * @param reserveB number of token B in the pair reserves
   * @param truePriceTokenA the nominator of the true price.
   * @param truePriceTokenB the denominator of the true price.
   */
  computeTradeToMoveMarket(
    truePriceTokenA: BigNumberish,
    truePriceTokenB: BigNumberish,
    reserveA: BigNumberish,
    reserveB: BigNumberish,
    overrides?: CallOverrides
  ): Promise<[boolean, BigNumber] & { aToB: boolean; amountIn: BigNumber }>;

  /**
   * Note that this method uses the Babylonian square root method which has a small margin of error which will result in a small over or under estimation on the size of the trade needed.
   * Given the "true" price a token (represented by truePriceTokenA/truePriceTokenB) and the reservers in the uniswap pair, calculate: a) the direction of trade (aToB) and b) the amount needed to trade (amountIn) to move the pool price to be equal to the true price.
   * @param reserveA number of token A in the pair reserves
   * @param reserveB number of token B in the pair reserves
   * @param truePriceTokenA the nominator of the true price.
   * @param truePriceTokenB the denominator of the true price.
   */
  "computeTradeToMoveMarket(uint256,uint256,uint256,uint256)"(
    truePriceTokenA: BigNumberish,
    truePriceTokenB: BigNumberish,
    reserveA: BigNumberish,
    reserveB: BigNumberish,
    overrides?: CallOverrides
  ): Promise<[boolean, BigNumber] & { aToB: boolean; amountIn: BigNumber }>;

  getReserves(
    factory: string,
    tokenA: string,
    tokenB: string,
    overrides?: CallOverrides
  ): Promise<
    [BigNumber, BigNumber] & { reserveA: BigNumber; reserveB: BigNumber }
  >;

  "getReserves(address,address,address)"(
    factory: string,
    tokenA: string,
    tokenB: string,
    overrides?: CallOverrides
  ): Promise<
    [BigNumber, BigNumber] & { reserveA: BigNumber; reserveB: BigNumber }
  >;

  callStatic: {
    /**
     * True price is expressed in the ratio of token A to token B.The caller must approve this contract to spend whichever token is intended to be swapped.
     * Swaps an amount of either token such that the trade results in the uniswap pair's price being as close as possible to the truePrice.
     * @param deadline to limit when the trade can execute. If the tx is mined after this timestamp then revert.
     * @param maxSpendTokens array of unit to represent the max to spend in the two tokens.
     * @param swappedTokens array of addresses which are to be swapped. The order does not matter as the function will figure out which tokens need to be exchanged to move the market to the desired "true" price.
     * @param to recipient of the trade proceeds.
     * @param tradingAsEOA bool to indicate if the UniswapV2Broker is being called by a DSProxy or an EOA.
     * @param truePriceTokens array of unit used to represent the true price. 0th value is the numerator of the true price and the 1st value is the the denominator of the true price.
     * @param uniswapFactory address of the uniswap factory used to fetch current pair reserves.
     * @param uniswapRouter address of the uniswap router used to facilitate trades.
     */
    swapToPrice(
      tradingAsEOA: boolean,
      uniswapRouter: string,
      uniswapFactory: string,
      swappedTokens: [string, string],
      truePriceTokens: [BigNumberish, BigNumberish],
      maxSpendTokens: [BigNumberish, BigNumberish],
      to: string,
      deadline: BigNumberish,
      overrides?: CallOverrides
    ): Promise<void>;

    /**
     * True price is expressed in the ratio of token A to token B.The caller must approve this contract to spend whichever token is intended to be swapped.
     * Swaps an amount of either token such that the trade results in the uniswap pair's price being as close as possible to the truePrice.
     * @param deadline to limit when the trade can execute. If the tx is mined after this timestamp then revert.
     * @param maxSpendTokens array of unit to represent the max to spend in the two tokens.
     * @param swappedTokens array of addresses which are to be swapped. The order does not matter as the function will figure out which tokens need to be exchanged to move the market to the desired "true" price.
     * @param to recipient of the trade proceeds.
     * @param tradingAsEOA bool to indicate if the UniswapV2Broker is being called by a DSProxy or an EOA.
     * @param truePriceTokens array of unit used to represent the true price. 0th value is the numerator of the true price and the 1st value is the the denominator of the true price.
     * @param uniswapFactory address of the uniswap factory used to fetch current pair reserves.
     * @param uniswapRouter address of the uniswap router used to facilitate trades.
     */
    "swapToPrice(bool,address,address,address[2],uint256[2],uint256[2],address,uint256)"(
      tradingAsEOA: boolean,
      uniswapRouter: string,
      uniswapFactory: string,
      swappedTokens: [string, string],
      truePriceTokens: [BigNumberish, BigNumberish],
      maxSpendTokens: [BigNumberish, BigNumberish],
      to: string,
      deadline: BigNumberish,
      overrides?: CallOverrides
    ): Promise<void>;

    /**
     * Note that this method uses the Babylonian square root method which has a small margin of error which will result in a small over or under estimation on the size of the trade needed.
     * Given the "true" price a token (represented by truePriceTokenA/truePriceTokenB) and the reservers in the uniswap pair, calculate: a) the direction of trade (aToB) and b) the amount needed to trade (amountIn) to move the pool price to be equal to the true price.
     * @param reserveA number of token A in the pair reserves
     * @param reserveB number of token B in the pair reserves
     * @param truePriceTokenA the nominator of the true price.
     * @param truePriceTokenB the denominator of the true price.
     */
    computeTradeToMoveMarket(
      truePriceTokenA: BigNumberish,
      truePriceTokenB: BigNumberish,
      reserveA: BigNumberish,
      reserveB: BigNumberish,
      overrides?: CallOverrides
    ): Promise<[boolean, BigNumber] & { aToB: boolean; amountIn: BigNumber }>;

    /**
     * Note that this method uses the Babylonian square root method which has a small margin of error which will result in a small over or under estimation on the size of the trade needed.
     * Given the "true" price a token (represented by truePriceTokenA/truePriceTokenB) and the reservers in the uniswap pair, calculate: a) the direction of trade (aToB) and b) the amount needed to trade (amountIn) to move the pool price to be equal to the true price.
     * @param reserveA number of token A in the pair reserves
     * @param reserveB number of token B in the pair reserves
     * @param truePriceTokenA the nominator of the true price.
     * @param truePriceTokenB the denominator of the true price.
     */
    "computeTradeToMoveMarket(uint256,uint256,uint256,uint256)"(
      truePriceTokenA: BigNumberish,
      truePriceTokenB: BigNumberish,
      reserveA: BigNumberish,
      reserveB: BigNumberish,
      overrides?: CallOverrides
    ): Promise<[boolean, BigNumber] & { aToB: boolean; amountIn: BigNumber }>;

    getReserves(
      factory: string,
      tokenA: string,
      tokenB: string,
      overrides?: CallOverrides
    ): Promise<
      [BigNumber, BigNumber] & { reserveA: BigNumber; reserveB: BigNumber }
    >;

    "getReserves(address,address,address)"(
      factory: string,
      tokenA: string,
      tokenB: string,
      overrides?: CallOverrides
    ): Promise<
      [BigNumber, BigNumber] & { reserveA: BigNumber; reserveB: BigNumber }
    >;
  };

  filters: {};

  estimateGas: {
    /**
     * True price is expressed in the ratio of token A to token B.The caller must approve this contract to spend whichever token is intended to be swapped.
     * Swaps an amount of either token such that the trade results in the uniswap pair's price being as close as possible to the truePrice.
     * @param deadline to limit when the trade can execute. If the tx is mined after this timestamp then revert.
     * @param maxSpendTokens array of unit to represent the max to spend in the two tokens.
     * @param swappedTokens array of addresses which are to be swapped. The order does not matter as the function will figure out which tokens need to be exchanged to move the market to the desired "true" price.
     * @param to recipient of the trade proceeds.
     * @param tradingAsEOA bool to indicate if the UniswapV2Broker is being called by a DSProxy or an EOA.
     * @param truePriceTokens array of unit used to represent the true price. 0th value is the numerator of the true price and the 1st value is the the denominator of the true price.
     * @param uniswapFactory address of the uniswap factory used to fetch current pair reserves.
     * @param uniswapRouter address of the uniswap router used to facilitate trades.
     */
    swapToPrice(
      tradingAsEOA: boolean,
      uniswapRouter: string,
      uniswapFactory: string,
      swappedTokens: [string, string],
      truePriceTokens: [BigNumberish, BigNumberish],
      maxSpendTokens: [BigNumberish, BigNumberish],
      to: string,
      deadline: BigNumberish,
      overrides?: Overrides & { from?: string | Promise<string> }
    ): Promise<BigNumber>;

    /**
     * True price is expressed in the ratio of token A to token B.The caller must approve this contract to spend whichever token is intended to be swapped.
     * Swaps an amount of either token such that the trade results in the uniswap pair's price being as close as possible to the truePrice.
     * @param deadline to limit when the trade can execute. If the tx is mined after this timestamp then revert.
     * @param maxSpendTokens array of unit to represent the max to spend in the two tokens.
     * @param swappedTokens array of addresses which are to be swapped. The order does not matter as the function will figure out which tokens need to be exchanged to move the market to the desired "true" price.
     * @param to recipient of the trade proceeds.
     * @param tradingAsEOA bool to indicate if the UniswapV2Broker is being called by a DSProxy or an EOA.
     * @param truePriceTokens array of unit used to represent the true price. 0th value is the numerator of the true price and the 1st value is the the denominator of the true price.
     * @param uniswapFactory address of the uniswap factory used to fetch current pair reserves.
     * @param uniswapRouter address of the uniswap router used to facilitate trades.
     */
    "swapToPrice(bool,address,address,address[2],uint256[2],uint256[2],address,uint256)"(
      tradingAsEOA: boolean,
      uniswapRouter: string,
      uniswapFactory: string,
      swappedTokens: [string, string],
      truePriceTokens: [BigNumberish, BigNumberish],
      maxSpendTokens: [BigNumberish, BigNumberish],
      to: string,
      deadline: BigNumberish,
      overrides?: Overrides & { from?: string | Promise<string> }
    ): Promise<BigNumber>;

    /**
     * Note that this method uses the Babylonian square root method which has a small margin of error which will result in a small over or under estimation on the size of the trade needed.
     * Given the "true" price a token (represented by truePriceTokenA/truePriceTokenB) and the reservers in the uniswap pair, calculate: a) the direction of trade (aToB) and b) the amount needed to trade (amountIn) to move the pool price to be equal to the true price.
     * @param reserveA number of token A in the pair reserves
     * @param reserveB number of token B in the pair reserves
     * @param truePriceTokenA the nominator of the true price.
     * @param truePriceTokenB the denominator of the true price.
     */
    computeTradeToMoveMarket(
      truePriceTokenA: BigNumberish,
      truePriceTokenB: BigNumberish,
      reserveA: BigNumberish,
      reserveB: BigNumberish,
      overrides?: CallOverrides
    ): Promise<BigNumber>;

    /**
     * Note that this method uses the Babylonian square root method which has a small margin of error which will result in a small over or under estimation on the size of the trade needed.
     * Given the "true" price a token (represented by truePriceTokenA/truePriceTokenB) and the reservers in the uniswap pair, calculate: a) the direction of trade (aToB) and b) the amount needed to trade (amountIn) to move the pool price to be equal to the true price.
     * @param reserveA number of token A in the pair reserves
     * @param reserveB number of token B in the pair reserves
     * @param truePriceTokenA the nominator of the true price.
     * @param truePriceTokenB the denominator of the true price.
     */
    "computeTradeToMoveMarket(uint256,uint256,uint256,uint256)"(
      truePriceTokenA: BigNumberish,
      truePriceTokenB: BigNumberish,
      reserveA: BigNumberish,
      reserveB: BigNumberish,
      overrides?: CallOverrides
    ): Promise<BigNumber>;

    getReserves(
      factory: string,
      tokenA: string,
      tokenB: string,
      overrides?: CallOverrides
    ): Promise<BigNumber>;

    "getReserves(address,address,address)"(
      factory: string,
      tokenA: string,
      tokenB: string,
      overrides?: CallOverrides
    ): Promise<BigNumber>;
  };

  populateTransaction: {
    /**
     * True price is expressed in the ratio of token A to token B.The caller must approve this contract to spend whichever token is intended to be swapped.
     * Swaps an amount of either token such that the trade results in the uniswap pair's price being as close as possible to the truePrice.
     * @param deadline to limit when the trade can execute. If the tx is mined after this timestamp then revert.
     * @param maxSpendTokens array of unit to represent the max to spend in the two tokens.
     * @param swappedTokens array of addresses which are to be swapped. The order does not matter as the function will figure out which tokens need to be exchanged to move the market to the desired "true" price.
     * @param to recipient of the trade proceeds.
     * @param tradingAsEOA bool to indicate if the UniswapV2Broker is being called by a DSProxy or an EOA.
     * @param truePriceTokens array of unit used to represent the true price. 0th value is the numerator of the true price and the 1st value is the the denominator of the true price.
     * @param uniswapFactory address of the uniswap factory used to fetch current pair reserves.
     * @param uniswapRouter address of the uniswap router used to facilitate trades.
     */
    swapToPrice(
      tradingAsEOA: boolean,
      uniswapRouter: string,
      uniswapFactory: string,
      swappedTokens: [string, string],
      truePriceTokens: [BigNumberish, BigNumberish],
      maxSpendTokens: [BigNumberish, BigNumberish],
      to: string,
      deadline: BigNumberish,
      overrides?: Overrides & { from?: string | Promise<string> }
    ): Promise<PopulatedTransaction>;

    /**
     * True price is expressed in the ratio of token A to token B.The caller must approve this contract to spend whichever token is intended to be swapped.
     * Swaps an amount of either token such that the trade results in the uniswap pair's price being as close as possible to the truePrice.
     * @param deadline to limit when the trade can execute. If the tx is mined after this timestamp then revert.
     * @param maxSpendTokens array of unit to represent the max to spend in the two tokens.
     * @param swappedTokens array of addresses which are to be swapped. The order does not matter as the function will figure out which tokens need to be exchanged to move the market to the desired "true" price.
     * @param to recipient of the trade proceeds.
     * @param tradingAsEOA bool to indicate if the UniswapV2Broker is being called by a DSProxy or an EOA.
     * @param truePriceTokens array of unit used to represent the true price. 0th value is the numerator of the true price and the 1st value is the the denominator of the true price.
     * @param uniswapFactory address of the uniswap factory used to fetch current pair reserves.
     * @param uniswapRouter address of the uniswap router used to facilitate trades.
     */
    "swapToPrice(bool,address,address,address[2],uint256[2],uint256[2],address,uint256)"(
      tradingAsEOA: boolean,
      uniswapRouter: string,
      uniswapFactory: string,
      swappedTokens: [string, string],
      truePriceTokens: [BigNumberish, BigNumberish],
      maxSpendTokens: [BigNumberish, BigNumberish],
      to: string,
      deadline: BigNumberish,
      overrides?: Overrides & { from?: string | Promise<string> }
    ): Promise<PopulatedTransaction>;

    /**
     * Note that this method uses the Babylonian square root method which has a small margin of error which will result in a small over or under estimation on the size of the trade needed.
     * Given the "true" price a token (represented by truePriceTokenA/truePriceTokenB) and the reservers in the uniswap pair, calculate: a) the direction of trade (aToB) and b) the amount needed to trade (amountIn) to move the pool price to be equal to the true price.
     * @param reserveA number of token A in the pair reserves
     * @param reserveB number of token B in the pair reserves
     * @param truePriceTokenA the nominator of the true price.
     * @param truePriceTokenB the denominator of the true price.
     */
    computeTradeToMoveMarket(
      truePriceTokenA: BigNumberish,
      truePriceTokenB: BigNumberish,
      reserveA: BigNumberish,
      reserveB: BigNumberish,
      overrides?: CallOverrides
    ): Promise<PopulatedTransaction>;

    /**
     * Note that this method uses the Babylonian square root method which has a small margin of error which will result in a small over or under estimation on the size of the trade needed.
     * Given the "true" price a token (represented by truePriceTokenA/truePriceTokenB) and the reservers in the uniswap pair, calculate: a) the direction of trade (aToB) and b) the amount needed to trade (amountIn) to move the pool price to be equal to the true price.
     * @param reserveA number of token A in the pair reserves
     * @param reserveB number of token B in the pair reserves
     * @param truePriceTokenA the nominator of the true price.
     * @param truePriceTokenB the denominator of the true price.
     */
    "computeTradeToMoveMarket(uint256,uint256,uint256,uint256)"(
      truePriceTokenA: BigNumberish,
      truePriceTokenB: BigNumberish,
      reserveA: BigNumberish,
      reserveB: BigNumberish,
      overrides?: CallOverrides
    ): Promise<PopulatedTransaction>;

    getReserves(
      factory: string,
      tokenA: string,
      tokenB: string,
      overrides?: CallOverrides
    ): Promise<PopulatedTransaction>;

    "getReserves(address,address,address)"(
      factory: string,
      tokenA: string,
      tokenB: string,
      overrides?: CallOverrides
    ): Promise<PopulatedTransaction>;
  };
}
